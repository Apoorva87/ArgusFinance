[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "argusfinance"
version = "0.1.0"
description = "Local-first options research workbench"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "alembic",
    "duckdb",
    "fastapi",
    "mcp",
    "pyarrow",
    "pydantic-settings",
    "sqlalchemy",
    "typer",
    "uvicorn",
]

[dependency-groups]
dev = [
    "httpx",
    "mypy",
    "pytest",
    "pytest-cov",
    "ruff",
]

[tool.hatch.build.targets.wheel]
packages = ["src/argusfinance"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.mypy]
files = ["src/argusfinance"]
python_version = "3.12"
strict = true
